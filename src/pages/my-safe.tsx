import Layout from "@/components/layout/layout";
import SafeModule from "@/components/safe/safe";
import { NextPage } from "next";

const MySafe: NextPage = ({ }) => {
    return <SafeModule />;
};

export default MySafe;